{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MockblogCategory list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Created</th>
                <th>Name</th>
                <th>Description</th>
                <th>Parentid</th>
                <th>Slug</th>
                <th>Picture</th>
                <th>Totalpost</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('mockblogcategory_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.parentId }}</td>
                <td>{{ entity.slug }}</td>
                <td>{{ entity.picture }}</td>
                <td>{{ entity.totalPost }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('mockblogcategory_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mockblogcategory_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('mockblogcategory_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
